---
import GuardIcon from "./GuardIcon.astro";

interface Props {
  title: string;
  description: string;
}

const { title, description } = Astro.props;
---

<header class="hero">
  <GuardIcon size={28} class="mark" />
  <h1>{title}</h1>
  <p class="lede">{description}</p>
</header>
